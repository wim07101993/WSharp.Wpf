<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:WSharp.Wpf.Controls;assembly=WSharp.Wpf">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/WSharp.Wpf.Material;component/Styles/ColorPicker/ColorPickerHorizontal.xaml" />
        <ResourceDictionary Source="pack://application:,,,/WSharp.Wpf.Material;component/Styles/ColorPicker/ColorPickerVertical.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style x:Key="ColorPicker" TargetType="{x:Type controls:ColorPicker}">
        <Style.Triggers>
            <Trigger Property="HueSliderPosition" Value="Top">
                <Setter Property="Template" Value="{StaticResource ColorPickerHorizontal}" />
            </Trigger>
            <Trigger Property="HueSliderPosition" Value="Bottom">
                <Setter Property="Template" Value="{StaticResource ColorPickerHorizontal}" />
            </Trigger>
            <Trigger Property="HueSliderPosition" Value="Left">
                <Setter Property="Template" Value="{StaticResource ColorPickerVertical}" />
            </Trigger>
            <Trigger Property="HueSliderPosition" Value="Right">
                <Setter Property="Template" Value="{StaticResource ColorPickerVertical}" />
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>